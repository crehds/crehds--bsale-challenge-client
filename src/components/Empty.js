const EMPTY_IMAGE = '../../src/assets/images/default-image-without_bg.png';

const Empty = () => `<div class="empty">
    <figure>
      <img src=${EMPTY_IMAGE} alt="Products not found" />
    </figure>
  </div>`;

export default Empty;
